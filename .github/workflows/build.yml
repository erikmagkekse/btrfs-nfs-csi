name: Build

on:
  push:
    branches: ["*"]
    tags: ["v*"]
    paths:
      - "**.go"
      - "go.mod"
      - "go.sum"
      - "Containerfile"
      - "VERSION"
      - ".github/workflows/build.yml"
  pull_request:
    branches: [main]
    paths:
      - "**.go"
      - "go.mod"
      - "go.sum"
      - "Containerfile"
      - "VERSION"
      - ".github/workflows/build.yml"

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/erikmagkekse/btrfs-nfs-csi

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Determine version
        id: version
        run: |
          BASE_VERSION=$(cat VERSION)
          REF=${GITHUB_REF_NAME}
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "version=${REF#v}" >> "$GITHUB_OUTPUT"
            echo "tag=${REF#v}" >> "$GITHUB_OUTPUT"
          elif [ "$REF" = "main" ]; then
            echo "version=${BASE_VERSION}" >> "$GITHUB_OUTPUT"
            echo "tag=${BASE_VERSION}" >> "$GITHUB_OUTPUT"
          else
            SAFE_BRANCH=$(echo "$REF" | sed 's/[^a-zA-Z0-9._-]/-/g')
            echo "version=${SAFE_BRANCH}-${BASE_VERSION}" >> "$GITHUB_OUTPUT"
            echo "tag=${SAFE_BRANCH}-${BASE_VERSION}" >> "$GITHUB_OUTPUT"
          fi
          echo "commit=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Build binary
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build \
            -ldflags "-X main.version=${{ steps.version.outputs.version }} -X main.commit=${{ steps.version.outputs.commit }}" \
            -o btrfs-nfs-csi .

      - name: Vet
        run: go vet ./...

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Containerfile
          push: true
          tags: ${{ env.IMAGE }}:${{ steps.version.outputs.tag }}
          build-args: |
            VERSION=${{ steps.version.outputs.version }}
            COMMIT=${{ steps.version.outputs.commit }}
